{
  "name": "calculator",
  "version": "1.0.0",
  "description": "Calculator Service listens for requests to perform math operations, and sends back a result.",
  "main": "lib/index.js",
  "scripts": {
    "build":"tsc",
    "start":"node lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy":"mkdir -p ./deploy && rush deploy -p calculator -s calculator -t ./deploy --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "~4.0.5",
    "@tsconfig/node14": "~1.0.0",
    "@types/node": "~14.14.6",
    "@types/rascal": "~10.0.3"
  },
  "dependencies": {
    "rascal": "~10.2.6",
    "amqplib": "~0.6.0"
  }
}
